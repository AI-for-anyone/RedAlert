# Core dependencies for all agent-hub projects
python-dotenv>=1.0.0

# LangChain dependencies for AI agents
langchain-openai
langchain-core
langchain_mcp_adapters
langchain

# MCP (Model Context Protocol) dependencies
mcp>=1.4.1,<1.7.0

# Mofa framework dependencies
# mofa

# Dora framework dependencies (for ra-terminal-input)
dora-rs  # Correct dora package (not the deprecated 'dora' with sklearn issues)

# Data processing dependencies (for ra-terminal-input)
numpy<2.0.0
pyarrow>=5.0.0

# Additional common dependencies
typing-extensions
dataclasses; python_version<"3.7"

# Development and testing
pytest
pytest-asyncio