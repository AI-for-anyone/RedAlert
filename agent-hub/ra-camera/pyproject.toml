[project]
name = "ra-camera"
version = "0.0.1"
authors = [
    {name = "Mofa Bot"}
]
description = "ra-camera"
license = {text = "MIT"}
dependencies = [
    "python-dotenv>=1.0.0"
]
requires-python = ">=3.10,<3.13"

[project.scripts]
ra-camera = "camera.main:main"

[tool.setuptools.packages.find]
include = ["camera*"]

[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"