[project]
name = "ra-produce"
version = "0.0.1"
authors = [
    {name = "Mofa Bot"}
]
description = "ra-produce"
license = {text = "MIT"}
dependencies = [
    "python-dotenv>=1.0.0"
]
requires-python = ">=3.10,<3.13"

[project.scripts]
ra-produce = "produce.main:main"

[tool.setuptools.packages.find]
include = ["produce*"]

[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"